version: '3.8'

services:
  epub-bot:
    image: jp0id/epub-bot:latest
    container_name: epub-bot
    restart: always
    environment:
      - TELEGRAM_BOT_TOKEN=你的Token
      - TELEGRAM_BOT_USERNAME=你的BotName
      - TELEGRAPH_AUTHOR_NAME=EpubReader
      - APP_CHARS_PER_PAGE=4000
      - BOT_ADMIN_ID=201728830,201728830
    volumes:
      - ./data:/app/data